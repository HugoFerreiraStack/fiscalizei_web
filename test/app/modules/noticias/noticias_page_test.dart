import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:fiscalizei_web/app/modules/noticias/noticias_page.dart';

void main() {
  testWidgets('NoticiasPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(NoticiasPage(title: 'Noticias')));
    //  final titleFinder = find.text('Noticias');
    //  expect(titleFinder, findsOneWidget);
  });
}
